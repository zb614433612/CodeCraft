# ============================================================
# 本地环境配置模板
# 复制本文件为 application-local.yml 并填入实际配置
# application-local.yml 已在 .gitignore 中，不会提交到 Git
#
# 为什么用这个文件？
# - application-local.yml 是 Spring Boot profile 配置，优先级高于 application.yml
# - 和项目在同一目录，方便管理
# - 比系统环境变量更直观，不需要修改系统配置
# ============================================================

# ⚠️ 注意：LLM Provider / API Key 不在此文件配置！
# 请启动后在页面「配置 → Provider 配置」中添加（存储于 llm_provider 表），
# 支持 DeepSeek / OpenAI / Anthropic / Ollama / MiMo 等平台。

# 代理配置（可选，国内访问海外 API 时使用）
network-tool:
  proxy:
    enabled: false
    host: 127.0.0.1
    port: 7890
